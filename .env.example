# Local Docker credentials are deliberately development-only.
# Copy to .env to change optional local demonstration settings.
DEMO_PASSWORD=FoodLink-demo-2026!
# Show role buttons that fill the default demo credentials on the sign-in page.
DEMO_LOGIN_ENABLED=true
