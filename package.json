{
  "name": "foodlink-workspace",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "format": "prettier --write backend/src backend/tests frontend/src scripts tests *.mjs *.json .github/workflows/*.yml docker-compose*.yml --no-error-on-unmatched-pattern",
    "format:check": "prettier --check backend/src backend/tests frontend/src scripts tests *.mjs *.json .github/workflows/*.yml docker-compose*.yml --no-error-on-unmatched-pattern",
    "test": "node scripts/test.mjs",
    "health": "node scripts/check-health.mjs"
  },
  "devDependencies": {
    "prettier": "3.9.6"
  }
}
